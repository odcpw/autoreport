# Local Library Placeholders

Place browser-ready local assets here for offline runs.

Expected candidates:
- pdf.js bundle
- mammoth browser bundle
- sheetjs browser bundle
- tesseract.js + language data
- transformers.js + onnxruntime-web (local paths)

Do not rely on CDN links in this experiment.
